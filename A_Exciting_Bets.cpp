#include <bits/stdc++.h>
#include <vector>
using namespace std;

long long solve(long long a,long long b){
    
}

int main(){
    int t;
    cin>>t;
    while(t--){
        long long a,b;
        cin>>a>>b;
        long long ans=solve(a, b);
        cout<<ans<<endl;
    }
}